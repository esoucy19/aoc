from collections.abc import Sequence


def make_new_molecules(
    molecule: str, replacements: Sequence[tuple[str, str]]
) -> set[str]:
    """Return all molecules from a molecule and replacements."""
    return set()
